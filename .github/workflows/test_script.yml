name: Test 

on:
  push:
  pull_request:
jobs:
  client:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Make
        run: make
      - name: Run Tests
        run: ./script.sh 1
